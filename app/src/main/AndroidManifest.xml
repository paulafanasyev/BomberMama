<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".BomberMamaApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:hardwareAccelerated="true"
        android:theme="@style/Theme.BomberMama"
        tools:targetApi="34">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.BomberMama.Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".game.GameActivity"
            android:exported="false"
            android:theme="@style/Theme.BomberMama.Game" />

        <activity
            android:name=".ui.LevelsActivity"
            android:exported="false"
            android:theme="@style/Theme.BomberMama.Menu" />

        <activity
            android:name=".ui.RecordsActivity"
            android:exported="false"
            android:theme="@style/Theme.BomberMama.Menu" />

        <activity
            android:name=".ui.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.BomberMama.Menu" />

        <activity
            android:name=".ui.HelpActivity"
            android:exported="false"
            android:theme="@style/Theme.BomberMama.Menu" />

    </application>

</manifest>
